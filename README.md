# DjangoPortfolio
A dynamic portfolio website built with Django. The frontend uses HTML, CSS, and JavaScript, while the backend is developed with Django and SQLite to manage dynamic content efficiently.
